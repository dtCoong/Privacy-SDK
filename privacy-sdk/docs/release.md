```markdown
# Release & scripts

This page has been merged into `docs/overview.md`.

See `docs/overview.md` for release instructions and a safe release checklist. The repo also includes `packages/contracts/scripts/release.sh` and `packages/contracts/scripts/release.ps1` for scripted releases.
Included scripts
```
