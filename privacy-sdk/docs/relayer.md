````markdown
# Relayer (`packages/relayer`)

This document describes the minimal relayer demo included in the monorepo.

Overview

- `packages/relayer` contains a small node service used in integration tests to demonstrate how the SDK and contracts interact.

Running the relayer (demo)

```cmd
cd packages\relayer
node server.js
```

This page has been merged into `docs/overview.md`.

See `docs/overview.md` for the relayer demo and running instructions.
````
